{
  "name": "gerenciar-gastos",
  "private": true,
  "scripts": {
    "dev:clean": "powershell -NoProfile -Command \"$ErrorActionPreference='SilentlyContinue'; $p = Get-NetTCPConnection -LocalPort 3000,3001 -State Listen | Select-Object -Expand OwningProcess -Unique; if ($p) { $p | ForEach-Object { Stop-Process -Id $_ -Force }; Start-Sleep 2 }; exit 0\"",
    "predev": "npm run dev:clean",
    "dev": "concurrently -n back,front -c blue,green \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
